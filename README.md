# alternateExampleDagsDirectory
These python files will be put directly into the dags folder of Airflow. Each of these
files constructs its own DAG and constructs its own tasks using its own functions.
